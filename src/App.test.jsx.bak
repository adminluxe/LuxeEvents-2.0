import { render, screen } from '@testing-library/react';
import App from './App';

test('renders sans planter', () => {
  render(<App />);
  expect(screen.getByText(/LuxeEvents/i)).toBeInTheDocument();
});
